//  Write a program to take input(age, name, phone no.) from user and display it

function UserInfo(name, age, phone_number) {
    return "Name " + name + " age " + age + " no. " + phone_number;
}
const user = UserInfo("Samarth", 12, 22222)
console.log(user)
